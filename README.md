## Project Source Code

This repository is organized into separate branches for each part of the system:

- `backend` : Backend source code
- `frontend` : Frontend source code
- `main` : Main repository information only

Please switch to the corresponding branch to view the project source code.

Example:
git checkout backend
